package frc.robot.model;

public enum ElevatorTiltPosition {
    IN, //the mechanism is angled into the frame perimeter 
    OUT //the mechanism is extended beyond the frame perimeter (the elevator is in vertical/perpendicular position)
}
